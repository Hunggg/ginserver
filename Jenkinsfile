/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/Hunggg/ginserver.git'
            }
        }
    }
}
